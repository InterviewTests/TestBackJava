server:
  port: 8090

spring:
  profiles:
    active: default      
  datasource: 
    username: root
    password: root
    url: jdbc:mysql://mysql-usuarios:3306/db_usuarios 
  application:
    name: 'auth'
  jpa:
    hibernate:
      ddl-auto: none
